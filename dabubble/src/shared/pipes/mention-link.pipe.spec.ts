import { MentionLinkPipe } from './mention-link.pipe';

describe('MentionLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new MentionLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
